# API Reference Guide: vector-index-ann-engine

## Module `hnsw_graph`

Detailed endpoint, RPC, and interface contracts for `hnsw_graph`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

## Module `distance_metrics`

Detailed endpoint, RPC, and interface contracts for `distance_metrics`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

## Module `quantization_pq`

Detailed endpoint, RPC, and interface contracts for `quantization_pq`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

## Module `index_serializer`

Detailed endpoint, RPC, and interface contracts for `index_serializer`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

## Module `search_router`

Detailed endpoint, RPC, and interface contracts for `search_router`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

## Module `dimension_reducer`

Detailed endpoint, RPC, and interface contracts for `dimension_reducer`.

### `initialize()`
Executes `initialize` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `verify_credentials()`
Executes `verify_credentials` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `execute_operation()`
Executes `execute_operation` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `sync_state()`
Executes `sync_state` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `rollback_transaction()`
Executes `rollback_transaction` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

### `export_metrics()`
Executes `export_metrics` with correlation context and strict validation.

```json
{
  "status": "OK",
  "retries": 0,
  "latency_us": 120
}
```

